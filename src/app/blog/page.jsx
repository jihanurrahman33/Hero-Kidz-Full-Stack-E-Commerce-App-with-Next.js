import { BlogPage } from "@/features/blog";
import React from "react";

export const metadata = {
  title: "Blog",
  description: "Read articles about kids learning and child development.",
};

const Page = () => {
    return <BlogPage />;
};

export default Page;
