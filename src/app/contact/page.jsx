import { ContactForm } from "@/features/contact";
import React from "react";

export const metadata = {
    title: "Contact Us",
    description: "Get in touch with Hero Kidz for any queries or support.",
};

const ContactPage = () => {
  return <ContactForm />;
};

export default ContactPage;
